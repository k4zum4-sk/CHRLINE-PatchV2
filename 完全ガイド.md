# 🎯 CHRLINE-PatchV2 使い方の完全ガイド

このドキュメントは、CHRLINE-PatchV2を使ってLINEメッセージを送信するまでの**完全な手順**をまとめたものです。

---

## 📚 はじめに

CHRLINE-PatchV2は、非公式のLINE API ラッパーで、以下のことができます：

- 個人アカウントでのBOT化
- グループチャットでの自動応答
- オープンチャット(スクエア)での活動
- メッセージの自動送信
- 画像・スタンプの送信

⚠️ **注意:** これは非公式APIです。LINEの利用規約に違反する可能性があるため、**学習・研究目的のみ**での使用を推奨します。

---

## 🚀 クイックスタート（5分で始める）

### 最小限の手順

```bash
# 1. リポジトリをクローン
git clone https://github.com/k4zum4-sk/CHRLINE-PatchV2.git
cd CHRLINE-PatchV2

# 2. 依存関係をインストール
pip install -r requirements.txt

# 3. CHRLINEをインストール
python setup.py install

# 4. セットアップ確認
python check_setup.py

# 5. ログイン
python simple_login.py

# 6. メッセージ送信
python simple_send_message.py
```

---

## 📖 詳細なドキュメント

### 初心者向け

1. **[使い方ガイド.md](使い方ガイド.md)** - 日本語の詳細ガイド
   - インストールから使い方まで画像付きで解説
   - よくある質問と答え
   - トラブルシューティング

2. **[QUICK_START_GUIDE.md](QUICK_START_GUIDE.md)** - English quick start guide
   - Installation instructions
   - Login and authentication
   - Sending messages

### 用意されているスクリプト

| スクリプト名 | 説明 | 使用例 |
|------------|------|--------|
| `check_setup.py` | セットアップの確認 | `python check_setup.py` |
| `simple_login.py` | ログインとトークン取得 | `python simple_login.py` |
| `simple_send_message.py` | メッセージ送信 | `python simple_send_message.py` |
| `complete_workflow.py` | 完全なワークフロー | `python complete_workflow.py` |
| `sample_usage.py` | サンプルコード集 | `python sample_usage.py` |

---

## 🔧 インストール手順（詳細）

### 1. Pythonのバージョン確認

```bash
python --version
# または
python3 --version
```

Python 3.6以上が必要です。

### 2. リポジトリのクローン

```bash
git clone https://github.com/k4zum4-sk/CHRLINE-PatchV2.git
cd CHRLINE-PatchV2
```

### 3. 依存関係のインストール

**推奨方法（requirements.txtを使用）:**

```bash
pip install -r requirements.txt
```

**個別インストール:**

```bash
pip install pycryptodomex
pip install xxhash
pip install "httpx[http2]"
pip install gevent
pip install thrift
pip install rsa
pip install python-axolotl-curve25519
pip install pillow
pip install requests
pip install qrcode
pip install cryptography
pip install rich
```

### 4. CHRLINEのインストール

```bash
python setup.py install
```

**権限エラーが出た場合:**

```bash
pip install --user .
```

### 5. インストールの確認

```bash
python check_setup.py
```

すべての項目に✅が表示されればOKです。

---

## 🔐 ログイン方法

### 方法1: QRコードログイン（推奨）

```bash
python simple_login.py
```

1. `1`を選択
2. QRコードが生成される
3. LINEアプリで **設定 → 他のデバイスでログイン** からQRコードをスキャン
4. 認証トークンが表示される

### 方法2: メール&パスワードログイン

```bash
python simple_login.py
```

1. `2`を選択
2. メールアドレスとパスワードを入力
3. 認証トークンが表示される

### トークンの保存

取得したトークンは環境変数に保存することを推奨：

**Mac/Linux:**
```bash
export LINE_AUTH_TOKEN="your_token_here"
```

**Windows PowerShell:**
```powershell
$env:LINE_AUTH_TOKEN = "your_token_here"
```

---

## 📨 メッセージ送信

### 基本的な流れ

1. **友達一覧を取得**

```bash
python simple_send_message.py
```

`1`を選択すると、友達一覧とそのIDが表示されます。

2. **メッセージを送信**

```bash
python simple_send_message.py
```

`2`を選択して、送信先のIDとメッセージを入力します。

### Pythonコードでの使用例

```python
from CHRLINE import CHRLINE
import os

# トークンでログイン
token = os.getenv("LINE_AUTH_TOKEN")
cl = CHRLINE(token, device="DESKTOPWIN")

# メッセージ送信
to_id = "u1234567890abcdef"
message = "こんにちは！"
cl.sendMessage(to_id, message)
```

---

## 🎨 応用例

### グループBOT

`test/group_bot.py`に完全なサンプルがあります：

- 特定のメッセージに自動応答
- 画像の送信
- スタンプの送信

### オープンチャットBOT

`test/square_bot.py`に完全なサンプルがあります：

- オープンチャットでのメッセージ検知
- 音声ファイルの送信
- メッセージの削除

### カスタムBOT

`sample_usage.py`に様々な機能のサンプルがあります：

- プロフィール取得
- グループ情報取得
- 画像送信

---

## ❓ よくある質問

### Q1: トークンはどのくらい有効？

**A:** 通常、数ヶ月間有効ですが、セキュリティ上の理由で無効になることがあります。

### Q2: 複数のアカウントを使える？

**A:** はい。各アカウントごとに異なるトークンを取得すれば使えます。

### Q3: スタンプや画像も送れる？

**A:** はい。`sample_usage.py`に画像送信のサンプルがあります。

### Q4: エラーが出た場合は？

**A:** 以下を確認してください：

1. `python check_setup.py`でセットアップを確認
2. トークンが有効か確認
3. インターネット接続を確認
4. [使い方ガイド.md](使い方ガイド.md)のトラブルシューティングを参照

---

## 🐛 トラブルシューティング

### よくあるエラーと解決方法

#### `No module named 'Crypto'`

```bash
pip uninstall pycrypto
pip install pycryptodomex
```

#### `No module named 'CHRLINE'`

```bash
cd /path/to/CHRLINE-PatchV2
python setup.py install
```

#### `No module named 'zope.event'`

```bash
pip install --force-reinstall zope.event zope.interface
pip install --force-reinstall gevent
```

#### `認証トークンが見つかりません`

1. `python simple_login.py`で新しいトークンを取得
2. 環境変数または`.line_token`ファイルに保存

#### セットアップ全体の確認

```bash
python check_setup.py
```

このスクリプトで全ての依存関係とインストール状況を確認できます。

---

## 📂 ファイル構成

```
CHRLINE-PatchV2/
├── README.md                    # メインのREADME
├── 使い方ガイド.md               # このファイル
├── QUICK_START_GUIDE.md         # 英語のクイックスタート
├── requirements.txt             # 依存関係リスト
├── check_setup.py               # セットアップ確認スクリプト
├── simple_login.py              # ログインスクリプト
├── simple_send_message.py       # メッセージ送信スクリプト
├── complete_workflow.py         # 完全なワークフロー
├── sample_usage.py              # サンプルコード集
├── test/                        # テストコード
│   ├── login_getToken_test.py
│   ├── sendMessage_test.py
│   ├── group_bot.py             # グループBOTサンプル
│   └── square_bot.py            # オープンチャットBOTサンプル
└── CHRLINE/                     # メインライブラリ
```

---

## 🔐 セキュリティに関する重要な注意

1. **トークンは絶対に公開しない**
   - GitHubにコミットしない
   - SNSに投稿しない
   - 他人に教えない

2. **トークンファイルを保護する**
   - `.line_token`は`.gitignore`に追加済み
   - 環境変数の使用を推奨

3. **定期的にトークンを更新**
   - 不正使用の疑いがある場合はすぐに変更

---

## ⚖️ 法的な注意事項

- これは**非公式のLINE API**です
- **LINEの利用規約に違反する可能性**があります
- 使用は**完全に自己責任**です
- **学習・研究目的のみ**での使用を強く推奨
- 商用利用や不正な用途には使用しないでください

---

## 📞 サポート

### ドキュメント

- 📖 [使い方ガイド.md](使い方ガイド.md) - 日本語の詳細ガイド
- 📖 [QUICK_START_GUIDE.md](QUICK_START_GUIDE.md) - English guide
- 📂 `test/` ディレクトリ - サンプルコード
- 📂 `examples/` ディレクトリ - 高度な使用例

### 問題が解決しない場合

1. `python check_setup.py`でセットアップを確認
2. [使い方ガイド.md](使い方ガイド.md)のトラブルシューティングを確認
3. GitHubのIssuesで質問: https://github.com/k4zum4-sk/CHRLINE-PatchV2/issues

---

## 🎉 まとめ

### 基本的な使い方（3ステップ）

1. **インストール**
   ```bash
   pip install -r requirements.txt
   python setup.py install
   ```

2. **ログイン**
   ```bash
   python simple_login.py
   ```

3. **メッセージ送信**
   ```bash
   python simple_send_message.py
   ```

### 次のステップ

- `test/group_bot.py` - グループBOTを試す
- `test/square_bot.py` - オープンチャットBOTを試す
- `sample_usage.py` - 様々な機能を試す
- オリジナルのBOTを作成する

---

**Happy Coding! 🚀**

質問や問題がある場合は、GitHubのIssuesでお気軽にご連絡ください。

元のCHRLINEライブラリの開発者: [DeachSword](https://github.com/DeachSword/CHRLINE)
