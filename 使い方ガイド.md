# 🚀 CHRLINE-PatchV2 使い方ガイド

このガイドでは、CHRLINE-PatchV2を使ってLINEメッセージを送信するまでの**完全な手順**を、初心者の方でも分かりやすく説明します。

---

## 📖 目次

1. [必要なもの](#必要なもの)
2. [インストール手順](#インストール手順)
3. [ログイン方法](#ログイン方法)
4. [メッセージ送信](#メッセージ送信)
5. [よくある質問](#よくある質問)
6. [トラブルシューティング](#トラブルシューティング)

---

## 必要なもの

- **Python 3.6以上**がインストールされていること
- **pip**（Pythonのパッケージマネージャー）
- **LINEアカウント**
- インターネット接続

### Pythonのバージョン確認

ターミナルまたはコマンドプロンプトで以下のコマンドを実行:

```bash
python --version
```

または

```bash
python3 --version
```

`Python 3.6.0`以上と表示されればOKです。

---

## インストール手順

### ステップ1: リポジトリをクローン

```bash
git clone https://github.com/k4zum4-sk/CHRLINE-PatchV2.git
cd CHRLINE-PatchV2
```

### ステップ2: 必要なパッケージをインストール

**推奨方法: requirements.txtを使用:**

```bash
pip install -r requirements.txt
```

**または、個別にインストール:**

以下のコマンドを**順番に**実行してください：

```bash
pip install pycryptodomex
pip install xxhash
pip install "httpx[http2]"
pip install gevent
pip install thrift
pip install rsa
pip install python-axolotl-curve25519
pip install pillow
pip install requests
pip install qrcode
pip install cryptography
pip install rich
```

**または一括インストール:**

```bash
pip install pycryptodomex xxhash "httpx[http2]" gevent thrift rsa python-axolotl-curve25519 pillow requests qrcode cryptography rich
```

### ステップ3: CHRLINEをインストール

```bash
python setup.py install
```

**エラーが出た場合は:**

```bash
pip install .
```

### インストールの確認

```bash
python check_setup.py
```

このスクリプトは、すべての依存関係が正しくインストールされているか確認します。

エラーが出なければ、インストール完了です！

---

## ログイン方法

### 🌟 方法1: QRコードログイン（推奨）

**最も簡単で安全な方法です。**

1. 以下のコマンドを実行:

```bash
python simple_login.py
```

2. プログラムが起動したら、`1`を入力してEnter

3. QRコードが生成されます（画像ファイルまたはターミナル上に表示）

4. **LINEアプリ**でQRコードをスキャン:
   - LINEアプリを開く
   - **設定** → **他のデバイスでログイン**
   - **QRコードをスキャン**

5. 認証が完了すると、**認証トークン**が表示されます:

```
🔑 認証トークン / Auth Token:
   EYJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

6. このトークンを**コピーして保存**してください。

7. プログラムが「トークンをファイルに保存しますか？」と聞いてきたら、`y`を入力すると`.line_token`ファイルに保存されます。

### 方法2: メールアドレスとパスワードでログイン

1. `simple_login.py`を実行:

```bash
python simple_login.py
```

2. `2`を選択

3. メールアドレスとパスワードを入力

4. 認証トークンが表示されます

### 方法3: 既存のトークンを使う

すでにトークンを持っている場合:

```bash
python simple_send_message.py
```

プログラムが自動的に以下からトークンを探します:
- 環境変数`LINE_AUTH_TOKEN`
- `.line_token`ファイル

---

## メッセージ送信

### ステップ1: 友達のIDを確認

```bash
python simple_send_message.py
```

1. `1`を選択（友達一覧を表示）
2. 友達の名前とIDが表示されます:

```
友達の詳細 / Friend details:
------------------------------------------------------------
1. 山田太郎
   ID: u1234567890abcdef1234567890abcdef
   Status: よろしくお願いします

2. 佐藤花子
   ID: u0987654321fedcba0987654321fedcba
   Status: 今日もいい天気！
```

3. メッセージを送りたい相手の**ID**をコピーします

### ステップ2: メッセージを送信

```bash
python simple_send_message.py
```

1. `2`を選択（メッセージを送信）
2. 送信先のIDを入力（コピーしたIDを貼り付け）
3. メッセージを入力
4. `y`を入力して送信確認

```
送信先のID / Recipient ID: u1234567890abcdef1234567890abcdef
メッセージ / Message: こんにちは！元気？

送信内容を確認してください / Confirm sending:
  宛先 / To: u1234567890abcdef1234567890abcdef
  メッセージ / Message: こんにちは！元気？

送信しますか？ / Send? (y/n): y

📤 送信中... / Sending...
✅ メッセージを送信しました! / Message sent successfully!
```

### 完全なワークフロー例

**ログインからメッセージ送信まで一気に実行**したい場合:

```bash
python complete_workflow.py
```

このスクリプトは以下を全て実行します:
1. ログイン
2. プロフィール表示
3. 友達一覧表示
4. メッセージ送信

---

## よくある質問

### Q1: トークンはどこに保存すればいい？

**A:** 以下の方法があります:

#### 方法1: 環境変数に設定（推奨）

**Mac/Linux:**
```bash
export LINE_AUTH_TOKEN="your_token_here"
```

永続的に保存する場合は、`~/.bashrc`または`~/.zshrc`に追加:
```bash
echo 'export LINE_AUTH_TOKEN="your_token_here"' >> ~/.bashrc
source ~/.bashrc
```

**Windows PowerShell:**
```powershell
$env:LINE_AUTH_TOKEN = "your_token_here"
```

永続的に保存:
```powershell
[System.Environment]::SetEnvironmentVariable("LINE_AUTH_TOKEN", "your_token_here", [System.EnvironmentVariableTarget]::User)
```

#### 方法2: .line_tokenファイルに保存

`simple_login.py`で自動的に保存できます。
ただし、**このファイルをGitにコミットしないよう注意！**

### Q2: トークンの有効期限は？

**A:** トークンは通常、数ヶ月間有効ですが、セキュリティ上の理由で無効になることがあります。無効になった場合は、再度ログインして新しいトークンを取得してください。

### Q3: 複数のアカウントを使える？

**A:** はい。各アカウントごとに異なるトークンを取得して使い分けることができます。

### Q4: グループやオープンチャットにもメッセージを送れる？

**A:** はい！グループやオープンチャットの場合は、`test/group_bot.py`や`test/square_bot.py`のサンプルを参考にしてください。

---

## トラブルシューティング

### エラー: `No module named 'Crypto'`

**解決方法:**

```bash
pip uninstall pycrypto
pip install pycryptodomex
```

### エラー: `No module named 'CHRLINE'`

**解決方法:**

```bash
cd /path/to/CHRLINE-PatchV2
python setup.py install
```

### エラー: `No module named 'zope.event'` (geventの問題)

**解決方法:**

```bash
pip install --force-reinstall zope.event zope.interface
pip install --force-reinstall gevent
```

または:

```bash
pip uninstall gevent
pip install "gevent[recommended]"
```

**さらに詳しい確認:**

```bash
python check_setup.py
```

このスクリプトで全ての依存関係をチェックできます。

### エラー: `認証トークンが見つかりません`

**解決方法:**

1. `python simple_login.py`を実行して新しいトークンを取得
2. 環境変数または`.line_token`ファイルにトークンを保存

### エラー: `ログイン失敗`

**原因と解決方法:**

1. **トークンが期限切れ**: 新しいトークンを取得
2. **インターネット接続の問題**: 接続を確認
3. **LINEのサーバーメンテナンス**: 時間をおいて再試行

### エラー: `QRコードが表示されない`

**解決方法:**

QRコードは以下の場所に保存されます:
- `CHRLINE/.images/` ディレクトリ内
- ファイル名: `login-{番号}.png`

手動で画像ファイルを開いてスキャンしてください。

### メッセージが送信されない

**チェック項目:**

1. 送信先のIDが正しいか確認
2. ログインが成功しているか確認
3. ネットワーク接続を確認
4. トークンが有効か確認

---

## 🔐 セキュリティに関する重要な注意

1. **トークンは絶対に公開しない**
   - SNSに投稿しない
   - Gitにコミットしない
   - 他人に教えない

2. **トークンファイルを保護する**
   - `.line_token`を`.gitignore`に追加済み
   - ファイルのパーミッションを適切に設定

3. **定期的にトークンを更新**
   - 不正使用の疑いがある場合はすぐに新しいトークンに変更

---

## ⚠️ 法的な注意事項

- これは**非公式のLINE API**です
- **LINEの利用規約に違反する可能性**があります
- 使用は**完全に自己責任**です
- **学習・研究目的のみ**での使用を強く推奨します
- 商用利用や不正な用途には使用しないでください

---

## 📚 さらに学習

### サンプルコード

- **基本的な使い方**: `test/`ディレクトリ
- **グループBOT**: `test/group_bot.py`
- **オープンチャットBOT**: `test/square_bot.py`
- **高度な使用例**: `examples/`ディレクトリ

### 利用可能なAPI

`CHRLINE/services/`ディレクトリを参照して、使用可能なAPIメソッドを確認してください。

---

## 🆘 サポートが必要な場合

問題が解決しない場合は、GitHubのIssuesで質問してください:

**https://github.com/k4zum4-sk/CHRLINE-PatchV2/issues**

---

## 🎉 まとめ

これで、CHRLINE-PatchV2を使ってLINEメッセージを送信できるようになりました！

**基本的な流れ:**

1. `python simple_login.py` → トークン取得
2. `python simple_send_message.py` → メッセージ送信

**困ったら:**
- このガイドの「トラブルシューティング」を確認
- GitHubのIssuesで質問

**楽しいコーディングを！Happy Coding! 🚀**
